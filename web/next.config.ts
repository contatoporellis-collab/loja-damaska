import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // На тарифе с 1 vCPU on-demand оптимизация не тянет мобильный трафик:
    // много фото → очередь на оптимизацию → часть фото не прогружается, а CPU
    // не успевает отдавать остальную страницу. Отдаём исходники статикой
    // напрямую — они уже сжаты (~150 КБ), это быстро и надёжно.
    // (Вернуть оптимизацию можно, добавив CDN или подняв тариф до 2+ vCPU.)
    unoptimized: true,
  },
};

export default nextConfig;
